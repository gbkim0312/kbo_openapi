KBO 경기 결과 OpenAPI 서버 구현 지침

1. 목표

KBO 경기 일정과 결과를 수집하여 PostgreSQL에 저장하고, 외부 클라이언트가 REST API와 OpenAPI 문서를 통해 조회할 수 있는 서버를 구현한다.

API 요청이 들어올 때마다 KBO 사이트를 크롤링하지 않는다.

다음 흐름으로 구현한다.

Scheduler 또는 관리 API
    → KBO 데이터 수집
    → 원본 응답 저장
    → 파싱 및 정규화
    → PostgreSQL Upsert
    → REST API에서 DB 조회

초기 구현은 마이크로서비스가 아닌 헥사고날 아키텍처 기반 모듈러 모놀리스로 작성한다.

API 프로세스와 수집 Worker는 동일한 Docker 이미지를 사용하되 실행 명령만 분리한다.

⸻

2. 기술 스택

다음 기술을 사용한다.

Language        Python 3.13
API             FastAPI
Validation      Pydantic 2
ORM             SQLAlchemy 2 Async
Migration       Alembic
Database        PostgreSQL
HTTP Client     httpx
HTML Parser     BeautifulSoup4 또는 lxml
CLI             kbocli, 1차 fallback 전용
Browser         Playwright, 2차 fallback 전용
Scheduler       APScheduler
Configuration   pydantic-settings
Testing         pytest, pytest-asyncio
Lint            Ruff
Type Check      mypy
Container       Docker Compose

패키지 관리는 uv를 우선 사용한다.

kbocli와 Playwright는 처음부터 모든 요청에 사용하지 않는다.

수집 우선순위는 다음과 같다.

1. KBO 페이지가 내부적으로 호출하는 JSON/XHR 요청
2. 일반 HTTP 요청으로 HTML 수집 후 파싱
3. HTTP 전송 오류 또는 응답 구조 변경 시 kbocli 사용
4. HTTP와 kbocli가 모두 불가능할 때만 Playwright 사용

kbocli는 독립적인 공식 데이터 API가 아니라 내부적으로 웹 데이터에 의존할 수 있으므로, 주 데이터 소스가 아니라 장애 복구용 Adapter로 취급한다.

⸻

3. 핵심 설계 원칙

반드시 다음 원칙을 지킨다.

1. API 요청 처리 중 KBO 사이트를 직접 호출하지 않는다.
2. 수집한 원본 응답을 정규화된 경기 데이터와 별도로 저장한다.
3. KBO 전용 HTML 또는 JSON 파서를 application/domain 계층과 분리한다.
4. 동일 경기를 반복 수집해도 중복 데이터가 생성되지 않아야 한다.
5. 경기 점수나 상태가 변경되면 revision을 증가시키고 변경 이력을 저장한다.
6. 수집 실패와 실제 무경기 상태를 구분한다.
7. 모든 시간은 DB에 timezone-aware 값으로 저장한다.
8. KBO 경기 시각은 Asia/Seoul 기준으로 해석한다.
9. API 필드명은 camelCase를 사용한다.
10. Python 내부 변수와 함수는 snake_case를 사용한다.
11. 코드에 KBO 팀, 경기 상태, DOM selector를 불필요하게 흩어놓지 않는다.
12. 외부 API와 내부 관리 API를 분리한다.
13. 외부 API에 parser 오류, stack trace, DB 오류를 노출하지 않는다.
14. 수집 Adapter는 Fake 구현으로 교체할 수 있어야 한다.
15. 실제 KBO 사이트에 의존하지 않고 대부분의 테스트가 실행되어야 한다.
16. KboHttpSource를 기본 데이터 소스로 사용하고 KboCliSource를 1차 fallback으로 사용한다.
17. 정상적인 경기 0건 응답에는 fallback을 실행하지 않는다.
18. CLI 출력 형식과 명령 인자는 Adapter 내부에 캡슐화하고 application/domain 계층에 노출하지 않는다.
19. HTTP와 CLI가 동일 경기를 반환해도 중복 경기로 저장되지 않아야 한다.
20. CLI 실행 파일의 버전은 Docker 이미지에서 고정하고 실행 시마다 다운로드하지 않는다.

⸻

4. 프로젝트 구조

다음 구조를 기본으로 생성한다.

kbo-result-api/
├── app/
│   ├── main.py
│   │
│   ├── domain/
│   │   ├── entities/
│   │   │   ├── game.py
│   │   │   ├── team.py
│   │   │   └── collection_job.py
│   │   ├── enums/
│   │   │   ├── game_status.py
│   │   │   ├── league_type.py
│   │   │   └── collection_status.py
│   │   ├── services/
│   │   │   └── game_change_detector.py
│   │   └── exceptions.py
│   │
│   ├── application/
│   │   ├── ports/
│   │   │   ├── inbound/
│   │   │   │   ├── collect_games.py
│   │   │   │   ├── get_games.py
│   │   │   │   └── get_game.py
│   │   │   └── outbound/
│   │   │       ├── game_source.py
│   │   │       ├── game_repository.py
│   │   │       ├── team_repository.py
│   │   │       ├── raw_snapshot_repository.py
│   │   │       ├── collection_job_repository.py
│   │   │       └── collection_lock.py
│   │   ├── use_cases/
│   │   │   ├── collect_games.py
│   │   │   ├── get_games.py
│   │   │   ├── get_game.py
│   │   │   └── get_teams.py
│   │   └── dto/
│   │       ├── source_game.py
│   │       ├── game_query.py
│   │       └── collection_result.py
│   │
│   ├── adapters/
│   │   ├── inbound/
│   │   │   ├── api/
│   │   │   │   ├── v1/
│   │   │   │   │   ├── games.py
│   │   │   │   │   ├── teams.py
│   │   │   │   │   └── health.py
│   │   │   │   ├── internal/
│   │   │   │   │   └── collections.py
│   │   │   │   ├── schemas/
│   │   │   │   │   ├── game.py
│   │   │   │   │   ├── team.py
│   │   │   │   │   ├── collection.py
│   │   │   │   │   └── error.py
│   │   │   │   ├── dependencies.py
│   │   │   │   └── exception_handlers.py
│   │   │   ├── scheduler/
│   │   │   │   └── collection_scheduler.py
│   │   │   └── cli/
│   │   │       ├── collect_date.py
│   │   │       └── backfill.py
│   │   │
│   │   └── outbound/
│   │       ├── sources/
│   │       │   ├── kbo_http_source.py
│   │       │   ├── kbo_cli_source.py
│   │       │   ├── kbo_playwright_source.py
│   │       │   ├── composite_kbo_source.py
│   │       │   ├── fake_kbo_source.py
│   │       │   ├── parser/
│   │       │   │   ├── game_parser.py
│   │       │   │   ├── status_mapper.py
│   │       │   │   └── team_mapper.py
│   │       │   └── exceptions.py
│   │       ├── persistence/
│   │       │   ├── database.py
│   │       │   ├── models/
│   │       │   │   ├── game.py
│   │       │   │   ├── team.py
│   │       │   │   ├── game_revision.py
│   │       │   │   ├── raw_snapshot.py
│   │       │   │   └── collection_job.py
│   │       │   └── repositories/
│   │       │       ├── sqlalchemy_game_repository.py
│   │       │       ├── sqlalchemy_team_repository.py
│   │       │       ├── sqlalchemy_raw_snapshot_repository.py
│   │       │       └── sqlalchemy_collection_job_repository.py
│   │       └── lock/
│   │           └── postgres_collection_lock.py
│   │
│   ├── infrastructure/
│   │   ├── config.py
│   │   ├── logging.py
│   │   ├── middleware.py
│   │   └── container.py
│   │
│   └── bootstrap/
│       ├── create_app.py
│       └── create_worker.py
│
├── migrations/
├── tests/
│   ├── unit/
│   ├── integration/
│   ├── contract/
│   ├── live/
│   └── fixtures/
│       └── kbo_responses/
├── scripts/
├── Dockerfile
├── compose.yml
├── pyproject.toml
├── alembic.ini
├── .env.example
├── Makefile
└── README.md

⸻

5. 도메인 모델

5.1 GameStatus

다음 표준 상태를 사용한다.

from enum import StrEnum
class GameStatus(StrEnum):
    SCHEDULED = "scheduled"
    PRE_GAME = "pre_game"
    IN_PROGRESS = "in_progress"
    DELAYED = "delayed"
    SUSPENDED = "suspended"
    POSTPONED = "postponed"
    CANCELED = "canceled"
    COMPLETED = "completed"
    UNKNOWN = "unknown"

KBO 원문 상태 문자열은 별도 필드인 source_status_text에 저장한다.

예:

sourceStatusText = "경기종료"
status = "completed"

알 수 없는 상태 문자열이 등장했다고 전체 수집을 실패시키지 않는다.

다음처럼 처리한다.

status = unknown
sourceStatusText = 원문
경고 로그 기록

5.2 LeagueType

최소 다음 값을 둔다.

class LeagueType(StrEnum):
    REGULAR = "regular"
    POSTSEASON = "postseason"
    EXHIBITION = "exhibition"
    ALL_STAR = "all_star"
    UNKNOWN = "unknown"

5.3 Game

도메인 경기 엔티티는 최소 다음 정보를 가진다.

id
source
source_game_id
season
league_type
game_date
scheduled_at
stadium
status
source_status_text
away_team_id
home_team_id
away_score
home_score
inning
result_text
winning_pitcher
losing_pitcher
save_pitcher
attendance
source_url
source_updated_at
first_collected_at
last_collected_at
revision
canonical_hash

점수는 경기 전에는 None일 수 있다.

점수 0과 값 없음 None을 구분한다.

⸻

6. Source DTO

KBO Adapter가 반환하는 결과는 ORM 모델이 아니라 SourceGame DTO여야 한다.

from dataclasses import dataclass
from datetime import date, datetime
@dataclass(frozen=True, slots=True)
class SourceGame:
    source: str
    source_game_id: str | None
    season: int
    league_type: LeagueType
    game_date: date
    scheduled_at: datetime | None
    stadium: str | None
    status: GameStatus
    source_status_text: str | None
    away_team_code: str
    away_team_name: str
    home_team_code: str
    home_team_name: str
    away_score: int | None
    home_score: int | None
    inning: str | None
    result_text: str | None
    winning_pitcher: str | None
    losing_pitcher: str | None
    save_pitcher: str | None
    attendance: int | None
    source_url: str | None
    source_updated_at: datetime | None

⸻

7. Port 인터페이스

7.1 GameSource

from datetime import date
from typing import Protocol
class GameSource(Protocol):
    async def fetch_games(
        self,
        target_date: date,
    ) -> list[SourceGame]:
        ...

필요한 경우에만 다음 메서드를 추가한다.

async def fetch_game(
    self,
    source_game_id: str,
) -> SourceGame | None:
    ...

7.2 GameRepository

다음 기능을 제공한다.

find_by_id
find_by_source_game_id
find_by_natural_key
find_games
upsert
save_revision

upsert는 단순 ORM merge()에 의존하지 말고 PostgreSQL의 INSERT ... ON CONFLICT DO UPDATE를 사용한다.

7.3 RawSnapshotRepository

다음 기능을 제공한다.

save
mark_parsed
mark_parse_failed
delete_older_than

7.4 CollectionLock

class CollectionLock(Protocol):
    async def acquire(
        self,
        lock_key: str,
    ) -> bool:
        ...
    async def release(
        self,
        lock_key: str,
    ) -> None:
        ...

PostgreSQL advisory lock으로 구현한다.

⸻

8. 데이터베이스 테이블

Alembic migration으로 다음 테이블을 생성한다.

8.1 teams

id                  bigint PK
code                varchar unique not null
name                varchar not null
short_name          varchar not null
active              boolean not null
created_at          timestamptz not null
updated_at          timestamptz not null

초기 migration 또는 seed에서 현재 KBO 구단 정보를 등록한다.

팀 매핑은 코드에만 의존하지 않고 DB의 teams 테이블을 기준으로 처리한다.

8.2 games

id                  bigint PK
source              varchar not null
source_game_id      varchar nullable
season              integer not null
league_type         varchar not null
game_date           date not null
scheduled_at        timestamptz nullable
stadium             varchar nullable
status              varchar not null
source_status_text  varchar nullable
away_team_id        bigint FK not null
home_team_id        bigint FK not null
away_score          integer nullable
home_score          integer nullable
inning              varchar nullable
result_text         varchar nullable
winning_pitcher     varchar nullable
losing_pitcher      varchar nullable
save_pitcher        varchar nullable
attendance          integer nullable
source_url          text nullable
source_updated_at   timestamptz nullable
first_collected_at  timestamptz not null
last_collected_at   timestamptz not null
revision            integer not null default 1
canonical_hash      varchar(64) not null
created_at          timestamptz not null
updated_at          timestamptz not null

다음 제약을 추가한다.

away_score >= 0
home_score >= 0
away_team_id != home_team_id
revision >= 1

source_game_id가 존재하면 다음 unique index를 사용한다.

unique(source, source_game_id)
where source_game_id is not null

공식 ID가 없는 경우를 위해 natural key도 둔다.

season
league_type
game_date
away_team_id
home_team_id
scheduled_at

단, double header를 구분할 수 있도록 scheduled_at 또는 별도의 game_number가 필요하다.

크롤링 결과에서 더블헤더 구분값을 얻을 수 있다면 game_number 필드를 추가한다.

8.3 game_revisions

id                  bigint PK
game_id             bigint FK not null
revision            integer not null
status              varchar not null
away_score          integer nullable
home_score          integer nullable
data                jsonb not null
changed_fields      jsonb not null
collected_at        timestamptz not null

다음 unique constraint를 둔다.

unique(game_id, revision)

8.4 raw_snapshots

id                  bigint PK
source              varchar not null
target_date         date nullable
request_url         text not null
request_method      varchar not null
request_params      jsonb nullable
response_status     integer nullable
response_headers    jsonb nullable
response_body       text not null
body_hash           varchar(64) not null
collected_at        timestamptz not null
parser_version      varchar nullable
parse_status        varchar not null
parse_error         text nullable

parse_status 값:

pending
succeeded
failed

민감한 헤더는 저장하지 않는다.

다음 항목은 반드시 제거한다.

Authorization
Cookie
Set-Cookie
Proxy-Authorization

8.5 collection_jobs

id                  uuid 또는 ulid PK
job_type            varchar not null
target_date         date not null
source              varchar not null
status              varchar not null
started_at          timestamptz nullable
finished_at         timestamptz nullable
attempt_count       integer not null
fetched_count       integer not null
inserted_count      integer not null
updated_count       integer not null
unchanged_count     integer not null
failed_count        integer not null
error_code          varchar nullable
error_message       text nullable
created_at          timestamptz not null
updated_at          timestamptz not null

상태:

pending
running
succeeded
partially_succeeded
failed

⸻

9. Canonical hash 및 변경 감지

경기의 변경 여부를 판단하기 위해 canonical payload를 만든다.

다음 필드만 포함한다.

status
source_status_text
scheduled_at
stadium
away_score
home_score
inning
result_text
winning_pitcher
losing_pitcher
save_pitcher
attendance

JSON key 정렬과 일정한 직렬화 방식을 적용한 뒤 SHA-256을 계산한다.

canonical_json = json.dumps(
    payload,
    ensure_ascii=False,
    sort_keys=True,
    separators=(",", ":"),
)
canonical_hash = hashlib.sha256(
    canonical_json.encode("utf-8"),
).hexdigest()

처리 규칙:

기존 경기 없음
    → games insert
    → revision = 1
    → game_revisions revision 1 생성
기존 경기 있음
기존 canonical_hash == 신규 canonical_hash
    → last_collected_at만 갱신
    → revision 변경 없음
기존 경기 있음
기존 canonical_hash != 신규 canonical_hash
    → games update
    → revision + 1
    → changed_fields 계산
    → game_revisions 추가

변경 감지와 revision 저장은 하나의 DB transaction에서 수행한다.

⸻

10. KBO Source Adapter

10.1 조사 단계

구현 전에 실제 KBO 경기 일정·결과 페이지의 네트워크 요청을 조사한다.

우선 다음을 확인한다.

경기 날짜 조회 시 호출되는 XHR 또는 fetch 요청
응답이 JSON인지 HTML fragment인지
공식 game ID 존재 여부
경기 상태 문자열
팀 코드
경기 시작 시간
더블헤더 구분값
우천취소 표현
포스트시즌 및 시범경기 구분법
사용 가능한 kbocli 명령 형식
kbocli의 JSON 출력 지원 여부
kbocli의 종료 코드와 stderr 형식
kbocli에서 무경기와 실행 실패를 구분하는 방법
kbocli가 제공하는 경기 ID와 필드 목록

가능하면 브라우저 DOM 자체를 파싱하지 말고 내부 데이터 요청을 직접 사용한다.

단, 비공개 인증 우회, CAPTCHA 우회, 접근 제한 회피는 구현하지 않는다.

10.2 KboHttpSource

다음 설정을 적용한다.

명확한 User-Agent
connect timeout
read timeout
전체 요청 timeout
재시도
지수 backoff
요청 간 최소 간격

권장 기본값:

connect timeout 5초
read timeout 15초
전체 timeout 20초
최대 재시도 3회
초기 backoff 1초

다음 오류는 재시도한다.

408
429
500
502
503
504
일시적 network error

다음 오류는 무조건 재시도하지 않는다.

400
401
403
404
파서 schema 불일치

단, 429 Retry-After가 있으면 해당 값을 우선 적용한다.

KboHttpSource가 다음 예외를 명시적으로 구분하도록 한다.

SourceTransportError
    연결 실패, timeout, 재시도 가능한 HTTP 오류

SourceSchemaChangedError
    필수 JSON 필드 또는 HTML 구조가 예상과 달라 파싱할 수 없는 경우

SourceNoGames
    정상 응답이며 해당 날짜에 실제 경기가 없는 경우

SourceConfigurationError
    잘못된 URL, 필수 설정 누락 등 운영 설정 오류

10.3 KboCliSource

KboCliSource는 KboHttpSource 실패 시 사용하는 1차 fallback Adapter다.

다음 경우에만 실행한다.

KboHttpSource의 연결 실패 또는 timeout
KBO 원본 서버의 재시도 가능한 5xx 응답
KBO 응답 구조 변경으로 인한 SourceSchemaChangedError

다음 경우에는 실행하지 않는다.

정상적인 경기 0건 응답
잘못된 날짜 입력
DB 저장 실패
내부 비즈니스 로직 오류
CLI 설정 누락 또는 잘못된 실행 파일 경로

CLI는 subprocess로 비대화형 실행한다.

asyncio.create_subprocess_exec를 사용한다.
shell=True 또는 문자열 기반 shell 실행을 사용하지 않는다.
stdout과 stderr를 분리해서 수집한다.
timeout을 적용하고 timeout 시 프로세스를 종료한다.
exit code가 0이 아니면 SourceTransportError 또는 SourceConfigurationError로 변환한다.
JSON 출력이 지원되는 경우에만 구조화된 SourceGame 변환에 사용한다.
사람이 읽는 텍스트 출력만 제공된다면 안정적인 parser fixture와 contract test를 반드시 작성한다.
CLI stdout 원문은 raw_snapshots에 source=kbo-cli로 저장한다.
stderr 전체를 API 응답에 노출하지 않는다.

CLI 명령 인자와 출력 형식은 실제 kbocli 문서를 확인한 뒤 구현한다.
확인하지 않은 `games --date --format json` 같은 인자를 임의로 작성하지 않는다.

Docker 이미지 빌드 시 kbocli 버전을 고정 설치한다.
실행 시마다 npx 또는 동적 다운로드로 패키지를 받아오지 않는다.

10.4 KboPlaywrightSource

HTTP 방식과 kbocli 방식으로 모두 데이터를 취득할 수 없을 때만 사용하는 2차 fallback이다.

조건:

HTTP 응답만으로 데이터 취득 불가
kbocli 실행 실패 또는 지원하지 않는 데이터 형식
JavaScript 실행 후에만 데이터 생성
내부 요청에 필요한 토큰이 브라우저 실행 중 생성

Playwright 구현에서도 DOM selector보다 브라우저가 호출하는 응답을 가로채는 방식을 우선한다.

10.5 CompositeKboSource

다음 규칙으로 구현한다.

KboHttpSource 성공
    → 결과 반환

KboHttpSource가 정상적인 무경기 결과
    → 빈 목록 반환
    → kbocli 및 Playwright fallback 금지

KboHttpSource가 SourceTransportError
    → KboCliSource 실행

KboHttpSource가 SourceSchemaChangedError
    → 설정에 따라 KboCliSource 실행
    → 경고 로그 기록

KboCliSource 성공
    → 결과 반환
    → fallback_used=true 기록

KboCliSource가 SourceTransportError 또는 SourceSchemaChangedError
    → Playwright가 활성화된 경우에만 KboPlaywrightSource 실행

모든 Source 실패
    → SourceUnavailableError 발생
    → 각 Adapter 실패 원인을 collection_job과 로그에 기록

0건과 파싱 실패를 명확히 구분한다.

CompositeKboSource는 각 Adapter가 반환한 SourceGame의 `source` 값을 보존한다.

예:

kbo-http
kbo-cli
kbo-playwright

단, 동일 경기 식별 시 Adapter 이름을 unique identity로 사용하지 않는다.
공식 source_game_id를 최우선으로 사용하고, 없으면 natural key로 중복을 방지한다.

⸻

11. Raw snapshot 저장 흐름

수집기는 응답을 받은 즉시 raw snapshot을 저장한다.

HTTP 응답 수신
    → raw snapshot parse_status=pending 저장
    → parser 실행
    → 성공 시 parse_status=succeeded
    → 실패 시 parse_status=failed, 오류 메시지 저장

응답 본문이 너무 크면 크기 제한을 둔다.

권장 최대 크기는 환경변수로 관리한다.

RAW_SNAPSHOT_MAX_BYTES=5242880

원본 보존 기간도 설정 가능하게 한다.

RAW_SNAPSHOT_RETENTION_DAYS=60

Adapter별 원본은 다음처럼 구분한다.

source=kbo-http
    response_body에 HTTP 응답 본문 저장

source=kbo-cli
    response_body에 CLI stdout 저장
    stderr는 민감 정보 제거 후 오류 요약만 parse_error 또는 collection_job에 저장

source=kbo-playwright
    가로챈 네트워크 응답 본문을 저장

⸻

12. 수집 Use Case

CollectGamesUseCase는 다음 순서로 동작한다.

1. target_date 유효성 검사
2. collection lock 획득
3. collection_job 생성
4. source.fetch_games(target_date)
   - CompositeKboSource가 HTTP → kbocli → Playwright 순서로 필요한 fallback을 수행한다.
   - 정상 무경기에서는 fallback을 수행하지 않는다.
5. 각 SourceGame 정규화 검증
6. 팀 code로 teams 조회
7. canonical hash 생성
8. game upsert
9. 필요 시 game_revision 생성
10. collection_job 결과 집계
11. transaction commit
12. lock 해제

수집 날짜 lock key:

kbo:collection:{source}:{target_date}

예:

kbo:collection:kbo:2026-08-05

이미 동일 날짜 수집이 진행 중이면 다음 오류를 반환한다.

COLLECTION_IN_PROGRESS

하나의 경기 파싱 실패가 전체 날짜 수집을 무조건 실패시키지 않도록 한다.

처리 가능한 경우:

정상 경기 4건
파싱 실패 경기 1건
→ partially_succeeded
→ fetched_count = 5
→ failed_count = 1

다만 페이지 전체 schema가 변경되어 모든 경기 파싱이 불가능한 경우에는 failed로 처리한다.

⸻

13. 외부 API

기본 prefix:

/api/v1

13.1 헬스 체크

GET /health/live
GET /health/ready

live는 프로세스 생존만 확인한다.

ready는 PostgreSQL 연결 가능 여부를 확인한다.

KBO 원본 사이트 접근 가능 여부는 readiness에 포함하지 않는다.

13.2 팀 목록

GET /api/v1/teams

응답:

{
    "teams": [
        {
            "code": "SS",
            "name": "삼성 라이온즈",
            "shortName": "삼성",
            "active": true
        }
    ]
}

13.3 경기 목록

GET /api/v1/games?date=2026-08-05

또는:

GET /api/v1/games?from=2026-08-01&to=2026-08-05

지원 필터:

date
from
to
team
status
leagueType
limit
cursor

검증 규칙:

date와 from/to를 동시에 허용하지 않는다.
from과 to는 함께 사용한다.
최대 조회 범위는 기본 31일이다.
limit 기본 50, 최대 200이다.

정렬 기본값:

scheduledAt ascending
id ascending

응답 예시:

{
    "games": [
        {
            "id": 123,
            "source": "kbo",
            "sourceGameId": "20260805LGSS0",
            "season": 2026,
            "leagueType": "regular",
            "gameDate": "2026-08-05",
            "scheduledAt": "2026-08-05T18:30:00+09:00",
            "stadium": "잠실",
            "status": "completed",
            "sourceStatusText": "경기종료",
            "awayTeam": {
                "code": "SS",
                "name": "삼성 라이온즈"
            },
            "homeTeam": {
                "code": "LG",
                "name": "LG 트윈스"
            },
            "score": {
                "away": 4,
                "home": 2
            },
            "inning": null,
            "winningPitcher": null,
            "losingPitcher": null,
            "savePitcher": null,
            "attendance": null,
            "revision": 3,
            "lastCollectedAt": "2026-08-05T22:19:12+09:00"
        }
    ],
    "meta": {
        "count": 1,
        "nextCursor": null,
        "collectedAt": "2026-08-05T22:19:12+09:00",
        "stale": false
    }
}

13.4 단일 경기

GET /api/v1/games/{game_id}

존재하지 않으면 HTTP 404와 GAME_NOT_FOUND를 반환한다.

13.5 최신 종료 경기

GET /api/v1/results/latest

선택적 query:

team
limit

completed 상태인 경기만 반환한다.

⸻

14. 내부 관리 API

기본 prefix:

/internal/v1

모든 요청은 다음 헤더를 요구한다.

Authorization: Bearer <ADMIN_API_KEY>

14.1 날짜별 수집

POST /internal/v1/collections

요청:

{
    "targetDate": "2026-08-05",
    "force": false
}

응답:

{
    "jobId": "0198876d-b198-7000-9000-123456789abc",
    "status": "succeeded",
    "targetDate": "2026-08-05",
    "fetchedCount": 5,
    "insertedCount": 1,
    "updatedCount": 2,
    "unchangedCount": 2,
    "failedCount": 0
}

초기 버전에서는 비동기 큐를 추가하지 말고 요청 내에서 수집을 완료해도 된다.

단, API handler에 수집 로직을 직접 작성하지 말고 반드시 use case를 호출한다.

14.2 수집 이력

GET /internal/v1/collections
GET /internal/v1/collections/{job_id}

14.3 과거 데이터 Backfill

POST /internal/v1/backfills

요청:

{
    "from": "2026-03-01",
    "to": "2026-08-05"
}

최대 범위를 제한한다.

초기 기본값:

최대 31일

대규모 backfill은 CLI를 우선 사용한다.

⸻

15. API 오류 형식

모든 오류는 다음 형식을 사용한다.

{
    "error": {
        "code": "GAME_NOT_FOUND",
        "message": "경기를 찾을 수 없습니다.",
        "details": {
            "gameId": "123"
        },
        "requestId": "0198876d-b198-7000-9000-123456789abc"
    }
}

필수 오류 코드:

INVALID_REQUEST
INVALID_DATE
INVALID_DATE_RANGE
DATE_RANGE_TOO_LARGE
GAME_NOT_FOUND
TEAM_NOT_FOUND
COLLECTION_IN_PROGRESS
SOURCE_UNAVAILABLE
SOURCE_PARSE_FAILED
UNAUTHORIZED
INTERNAL_ERROR

FastAPI 기본 validation 오류도 위 형식으로 변환한다.

⸻

16. 데이터 신선도

크롤링 기반 API이므로 응답에 마지막 수집 시각을 포함한다.

stale 기준은 설정 가능하게 구현한다.

기본값:

IN_PROGRESS       10분
PRE_GAME          15분
SCHEDULED         60분
COMPLETED         24시간
그 외             60분

응답 생성 시 현재 시각과 last_collected_at을 비교하여 계산한다.

⸻

17. Scheduler

APScheduler를 Worker 프로세스에서 실행한다.

초기 스케줄:

매일 00:10
    어제 경기 재수집
매일 06:00
    오늘 경기 일정 수집
매일 12:00
    오늘 경기 일정 재수집
매일 17:00부터 23:59까지
    5분 간격으로 오늘 경기 재수집

Cron 표현은 Asia/Seoul 기준으로 적용한다.

동일 날짜에 수동 수집과 스케줄 수집이 겹칠 수 있으므로 collection lock을 반드시 사용한다.

스케줄은 환경변수로 활성화 여부를 제어한다.

SCHEDULER_ENABLED=true

⸻

18. 설정값

.env.example을 다음 기준으로 작성한다.

APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
APP_LOG_LEVEL=INFO
TZ=Asia/Seoul
DATABASE_URL=postgresql+asyncpg://kbo:kbo@postgres:5432/kbo
ADMIN_API_KEY=change-me
KBO_SOURCE_MODE=composite
KBO_BASE_URL=https://www.koreabaseball.com
KBO_USER_AGENT=kbo-result-api/0.1
KBO_CONNECT_TIMEOUT_SECONDS=5
KBO_READ_TIMEOUT_SECONDS=15
KBO_TOTAL_TIMEOUT_SECONDS=20
KBO_MAX_RETRIES=3
KBO_HTTP_FALLBACK_ON_SCHEMA_ERROR=true
KBO_CLI_ENABLED=true
KBO_CLI_COMMAND=kbocli
KBO_CLI_TIMEOUT_SECONDS=30
KBO_CLI_FALLBACK_ON_SCHEMA_ERROR=true
KBO_PLAYWRIGHT_ENABLED=false
KBO_MIN_REQUEST_INTERVAL_SECONDS=1
RAW_SNAPSHOT_ENABLED=true
RAW_SNAPSHOT_MAX_BYTES=5242880
RAW_SNAPSHOT_RETENTION_DAYS=60
SCHEDULER_ENABLED=true
MAX_QUERY_RANGE_DAYS=31
DEFAULT_PAGE_SIZE=50
MAX_PAGE_SIZE=200

실제 KBO endpoint는 조사 후 별도 변수로 추가한다.

비밀값은 코드나 compose 파일에 직접 넣지 않는다.

⸻

19. 로깅

JSON 구조 로그를 사용한다.

모든 요청에 request_id를 부여한다.

수집 로그에는 다음 필드를 포함한다.

request_id
job_id
target_date
source
duration_ms
fetched_count
inserted_count
updated_count
unchanged_count
failed_count
source_adapter
fallback_used
fallback_chain
cli_exit_code

다음은 로그에 남기지 않는다.

ADMIN_API_KEY
Authorization 헤더
Cookie
전체 DB 접속 비밀번호

Parser 오류에는 다음 정보를 포함한다.

snapshot_id
parser_version
target_date
selector 또는 필드명
예외 타입

원본 전체 응답은 로그에 출력하지 않는다.

⸻

20. 테스트

20.1 Fixture

다음 fixture를 만든다.

tests/fixtures/kbo_responses/
├── regular_scheduled.html 또는 json
├── regular_in_progress.html 또는 json
├── regular_completed.html 또는 json
├── rain_canceled.html 또는 json
├── postponed.html 또는 json
├── double_header.html 또는 json
├── no_games.html 또는 json
├── unknown_status.html 또는 json
├── malformed_response.html 또는 json
├── cli_regular_completed.json
├── cli_no_games.json
├── cli_unknown_status.json
└── cli_malformed_output.txt

실제 응답을 fixture로 저장할 경우 개인정보나 불필요한 헤더를 제거한다.

20.2 Unit test

최소 다음 테스트를 작성한다.

KBO 상태 문자열이 GameStatus로 변환된다.
알 수 없는 상태가 UNKNOWN으로 변환된다.
팀 이름과 팀 코드가 정규화된다.
점수 0과 None이 구분된다.
경기 시각이 Asia/Seoul timezone-aware datetime으로 변환된다.
canonical hash가 필드 순서와 무관하게 동일하다.
경기 데이터 변경 시 changed_fields가 계산된다.
변경이 없으면 revision이 증가하지 않는다.
HTTP 정상 무경기에는 KboCliSource가 호출되지 않는다.
HTTP transport 오류에는 KboCliSource가 호출된다.
HTTP schema 변경 오류에는 설정에 따라 KboCliSource가 호출된다.
kbocli timeout 시 프로세스가 종료되고 SourceTransportError가 발생한다.
kbocli exit code 비정상 시 stderr가 외부 API에 노출되지 않는다.
HTTP와 CLI가 동일 source_game_id를 반환해도 중복 저장되지 않는다.

20.3 Parser contract test

각 fixture를 파싱하여 예상 SourceGame 목록과 비교한다.

HTTP 응답 parser와 kbocli 출력 parser를 서로 분리한다.
kbocli의 JSON 또는 텍스트 출력 형식이 변경되면 CLI contract test가 실패해야 한다.
DOM selector 또는 JSON 필드가 변경되면 contract test가 실패해야 한다.

20.4 Repository integration test

Testcontainers 또는 테스트 PostgreSQL을 사용한다.

최소 테스트:

신규 경기 insert
동일 경기 재수집 시 중복 방지
데이터 변경 시 revision 증가
변경 없음 시 last_collected_at만 갱신
source_game_id 기반 conflict 처리
natural key 기반 conflict 처리
동시 upsert 안전성

SQLite로 PostgreSQL 전용 동작을 대체하지 않는다.

20.5 API integration test

최소 테스트:

GET /health/live
GET /health/ready
GET /api/v1/teams
GET /api/v1/games?date=
GET /api/v1/games?from=&to=
GET /api/v1/games/{id}
날짜 범위 초과
잘못된 team
없는 game
관리 API 인증 실패
관리 API 수집 성공

20.6 Live smoke test

실제 KBO 사이트를 호출하는 테스트는 tests/live에 분리한다.

기본 테스트 실행에는 포함하지 않는다.

pytest
    → live test 제외
pytest -m live
    → 명시적으로 실행

⸻

21. Dockerfile

멀티 스테이지 빌드를 사용한다.

최종 이미지는 root가 아닌 사용자로 실행한다.

동일 이미지에서 다음 명령을 지원한다.

api
worker
collect-date
backfill
migrate

예:

docker run ... kbo-result-api:local api
docker run ... kbo-result-api:local worker
docker run ... kbo-result-api:local collect-date 2026-08-05

entrypoint script에서 명령을 분기해도 된다.

kbocli를 사용할 경우 Docker 이미지 빌드 단계에서 정확한 버전을 고정 설치한다.
실행 시점의 동적 설치는 금지한다.
kbocli 실행 파일이 PATH에 존재하는지 이미지 build 또는 smoke test에서 확인한다.

⸻

22. Docker Compose

다음 서비스를 만든다.

postgres
kbo-api
kbo-worker

kbo-api와 kbo-worker는 동일한 로컬 이미지를 사용한다.

예상 구조:

services:
    postgres:
        image: postgres:18
        container_name: kbo-postgres
        restart: unless-stopped
        environment:
            POSTGRES_DB: kbo
            POSTGRES_USER: kbo
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        volumes:
            - ${KBO_DATA_PATH}/postgres:/var/lib/postgresql/data
        healthcheck:
            test:
                - CMD-SHELL
                - pg_isready -U kbo -d kbo
            interval: 10s
            timeout: 5s
            retries: 5
    kbo-api:
        image: kbo-result-api:local
        build:
            context: .
        container_name: kbo-api
        restart: unless-stopped
        command:
            - api
        env_file:
            - .env
        ports:
            - "8085:8000"
        depends_on:
            postgres:
                condition: service_healthy
    kbo-worker:
        image: kbo-result-api:local
        container_name: kbo-worker
        restart: unless-stopped
        command:
            - worker
        env_file:
            - .env
        depends_on:
            postgres:
                condition: service_healthy

기본 이미지는 KboHttpSource와 KboCliSource를 모두 실행할 수 있어야 한다.

Playwright가 필요한 경우에는 별도 Docker Compose profile 또는 별도 이미지 변형을 사용한다.
기본 운영 순서는 HTTP → kbocli이며 Playwright는 기본적으로 비활성화한다.

⸻

23. Makefile 명령

최소 다음 명령을 제공한다.

make install
make lint
make format
make type-check
make test
make test-integration
make test-live
make build
make up
make down
make logs
make migrate
make migration
make collect DATE=2026-08-05

make check는 다음을 순서대로 실행한다.

ruff check
ruff format --check
mypy
pytest

⸻

24. README

README에 다음 내용을 포함한다.

프로젝트 목적
전체 아키텍처
로컬 실행 방법
환경변수 설명
DB migration 방법
API 실행 방법
Worker 실행 방법
수동 수집 방법
Swagger UI 위치
테스트 실행 방법
KBO Source fixture 갱신 방법
알려진 제한 사항

Swagger UI:

http://localhost:8085/docs

OpenAPI JSON:

http://localhost:8085/openapi.json

⸻

25. 구현 순서

다음 순서로 구현한다.

Phase 1. 프로젝트 기반

pyproject.toml
FastAPI app
환경 설정
로깅
PostgreSQL 연결
Alembic
Dockerfile
compose.yml
health endpoint

Phase 2. 도메인과 DB

GameStatus
LeagueType
Team
Game
GameRevision
RawSnapshot
CollectionJob
repository interface
SQLAlchemy repository
migration
team seed

Phase 3. Fake Source

실제 크롤러보다 먼저 FakeKboSource를 만든다.

Fake 데이터로 다음 흐름을 완성한다.

수집 API
→ SourceGame
→ Upsert
→ revision
→ games 조회 API

Phase 4. KBO Source 조사 및 구현

KBO 네트워크 요청 조사
HTTP 응답 fixture 저장
KboHttpSource 구현
HTTP parser contract test 작성
kbocli 명령 및 출력 형식 조사
kbocli 출력 fixture 저장
KboCliSource 구현
CLI parser contract test 작성
CompositeKboSource 구현
HTTP 정상 무경기와 fallback 조건 테스트
필요할 때만 KboPlaywrightSource 구현
상태 및 팀 mapping 작성

Phase 5. Scheduler와 운영 기능

APScheduler worker
PostgreSQL advisory lock
collection_jobs
raw snapshot cleanup
관리 API 인증

Phase 6. 품질 보강

API 오류 통일
request ID
cursor pagination
live smoke test
README
make check 통과

⸻

26. 완료 조건

다음 조건을 모두 만족해야 한다.

* docker compose up -d로 PostgreSQL, API, Worker가 실행된다.
* Alembic migration이 정상 적용된다.
* /health/live가 200을 반환한다.
* /health/ready가 DB 상태에 따라 적절히 응답한다.
* /docs에서 Swagger UI를 확인할 수 있다.
* /openapi.json을 받을 수 있다.
* 특정 날짜 경기 수집이 가능하다.
* 동일 날짜를 여러 번 수집해도 경기 중복이 생성되지 않는다.
* 점수가 변경되면 game revision이 증가한다.
* 변경이 없으면 revision이 증가하지 않는다.
* 원본 응답이 raw snapshot에 저장된다.
* 무경기와 parser 실패가 구분된다.
* 외부 API는 KBO 사이트를 직접 호출하지 않고 DB만 조회한다.
* 내부 관리 API는 ADMIN_API_KEY로 보호된다.
* Fake Source를 사용한 테스트가 가능하다.
* parser fixture 테스트가 존재한다.
* KboHttpSource가 기본 Source로 동작한다.
* HTTP 전송 또는 파싱 장애 시 KboCliSource fallback이 동작한다.
* 정상 무경기 응답에는 CLI fallback이 실행되지 않는다.
* kbocli 출력 fixture와 contract test가 존재한다.
* HTTP와 CLI 출처가 달라도 동일 경기가 중복 저장되지 않는다.
* PostgreSQL 기반 repository integration test가 존재한다.
* make check가 통과한다.
* README만 보고 로컬에서 실행할 수 있다.

⸻

27. 초기 MVP 제한

초기 버전에서는 다음 기능을 구현하지 않아도 된다.

실시간 문자중계
선수 개인 기록 전체
이닝별 상세 타석 정보
WebSocket
Kafka
Celery
Kubernetes
서로 다른 Source 결과의 점수 충돌 자동 판정 및 병합
사용자 계정
복잡한 Rate Limit
Redis

단, 향후 추가할 수 있도록 application port와 adapter 경계를 유지한다.

⸻

28. Agent 작업 방식

작업 도중 단순한 선택 사항 때문에 사용자에게 확인을 요청하지 말고 합리적인 기본값으로 진행한다.

실제 KBO 응답 구조가 예상과 다르면 다음 순서로 대응한다.

1. 실제 네트워크 요청 확인
2. HTTP 응답 fixture 저장
3. KboHttpSource parser 구현
4. HTTP contract test 추가
5. kbocli 명령과 출력 형식 확인
6. kbocli 출력 fixture 저장
7. KboCliSource parser와 Adapter 구현
8. CompositeKboSource fallback 테스트 추가
9. 필요한 경우에만 Playwright Adapter 구현

사이트 구조를 확인할 수 없는 환경이면 다음까지만 완성한다.

전체 서버 구조
DB와 migration
FakeKboSource
KboHttpSource 인터페이스와 placeholder
KboCliSource 인터페이스와 placeholder
CompositeKboSource와 fallback 정책 테스트
fixture 기반 parser
외부 및 내부 API
Docker Compose
테스트
README

확인하지 않은 KBO endpoint나 selector를 실제 값인 것처럼 임의 작성하지 않는다.

확인하지 못한 부분은 명시적인 TODO와 문서로 남긴다.

작업 완료 후 다음을 보고한다.

생성하거나 수정한 파일 목록
구현된 기능
실행 명령
테스트 결과
실제 KBO 연동 여부
남아 있는 TODO