# KBO OpenAPI 개선사항

현재 실제 서버에서 확인된 동작을 기준으로, 남은 개선 작업을 정리한 문서입니다.

## 현재 상태

다음 기능은 구현되어 실제 서버에서 동작하고 있습니다.

- KBO 경기 일정·상태·점수·이닝 수집
- 경기중 실시간 점수 수집
- 네이버 스포츠 fallback을 통한 이닝별 득점·안타·에러·볼넷 수집
- 확정 라인업·선발투수·WAR 수집
- 경기 공식 프리뷰 분석 수집
- 경기 상세 투수 기록·결승타 수집
- 팀 순위·승차·최근 10경기·연승/연패 수집
- 관리자 대시보드와 전체 수집 버튼
- PostgreSQL 저장, revision 및 원본 응답 보관

## 순차 개선 체크리스트

아래 순서대로 작업합니다. 기능 구현, 테스트, 서버 검증까지 끝난 항목만 `[x]`로 변경합니다.

### 0. 공통 준비

- [ ] 작업 전 현재 서버 API 응답과 DB 상태를 기록한다.
- [ ] 변경 전 테스트 fixture와 기존 API 하위 호환 여부를 확인한다.
- [ ] 기능 구현 후 단위 테스트·정적 검사를 실행한다.
- [ ] Docker 이미지 빌드 및 마이그레이션을 실행한다.
- [ ] 실제 서버 API로 수집 결과를 검증한다.
- [ ] README와 Swagger 문서를 갱신한다.
- [ ] 검증이 끝난 뒤 이 문서의 해당 항목을 `[x]`로 체크한다.

### 1. 기준 날짜 이후 최신 순위 fallback (P1)

- [x] `as_of_date <= 요청 날짜` 조건으로 최신 순위를 조회한다.
- [x] 요청 날짜에 정확한 스냅샷이 없을 때 이전 스냅샷을 반환한다.
- [x] 응답 `asOfDate`에 실제 사용한 스냅샷 날짜를 표시한다.
- [x] 이전 날짜 데이터를 사용했는지 확인할 수 있는 `stale` 또는 `dataAge` 메타데이터를 추가한다.
- [ ] 날짜 스냅샷이 전혀 없을 때의 빈 응답을 정의한다.
- [ ] 과거 날짜·오늘 날짜·미래 날짜 API 테스트를 추가한다.
- [ ] 실제 서버에서 `GET /api/v1/rankings?date=오늘`을 검증한다.

완료 기준:

```text
GET /api/v1/rankings?date=2026-09-19
→ asOfDate: 2026-09-18
→ rankings: 10개 팀
```

### 2. 종료 경기 최종 R/H/E/BB 보강 (P1)

- [x] 경기 종료 상태 전환을 감지한다.
- [x] 종료 직후 최종 스코어보드 수집을 요청한다.
- [x] KBO 상세 스코어보드 응답을 우선 사용한다.
- [x] KBO 응답이 비어 있으면 네이버 스포츠 fallback을 사용한다.
- [x] 실시간 scoreboard를 최종 scoreboard로 갱신한다.
- [x] `scoreboardSource`와 수집 시각을 보존한다.
- [ ] `/api/v1/results/latest`에 R/H/E/BB가 포함되는 테스트를 추가한다.
- [ ] 실제 종료 경기 API 응답을 검증한다.

완료 기준:

```json
{
  "score": {
    "away": 4,
    "home": 1,
    "hits": {"away": 8, "home": 5},
    "errors": {"away": 0, "home": 1},
    "walks": {"away": 3, "home": 2},
    "scoreboardSource": "kbo-scoreboard"
  }
}
```

### 3. 경기 투수 결과 표준화 (P1)

- [x] KBO 원문 `승`, `패`, `세`, `홀드` 필드 매핑을 정의한다.
- [x] `win`, `loss`, `save`, `hold` 표준 필드를 API에 추가한다.
- [x] 기존 `stats` 원문 필드는 하위 호환을 위해 유지한다.
- [x] 경기 요약에 승리투수·패전투수·세이브투수를 노출한다.
- [ ] 투수 결과가 없는 경우 `null` 처리 기준을 정의한다.
- [ ] 완료 경기 fixture와 API 테스트를 추가한다.
- [ ] 실제 서버의 경기 상세 응답을 검증한다.

### 4. Swagger 응답 모델 확대 (P2)

- [ ] 팀 목록 응답 모델을 추가한다.
- [ ] 순위 응답 모델을 추가한다.
- [ ] 선수 기록 응답 모델을 추가한다.
- [ ] MVP 응답 모델을 추가한다.
- [ ] 경기 상세·라인업·분석 응답 모델을 추가한다.
- [ ] 내부 수집 API 응답 모델을 추가한다.
- [ ] 공통 오류 응답 스키마를 연결한다.
- [ ] `/openapi.json`에 실제 필드가 노출되는지 확인한다.

### 5. 수집 출처 추상화 확대 (P2)

- [ ] KBO HTTP, 네이버 스포츠, 향후 공급자용 공통 Port를 정의한다.
- [ ] 공급자별 요청 성공률과 실패 원인을 기록한다.
- [ ] 공급자별 마지막 성공 수집 시각을 기록한다.
- [ ] 네이버 endpoint 변경 시 어댑터만 교체할 수 있는지 확인한다.
- [ ] 공급자 선택 설정과 fallback 순서를 README에 문서화한다.

### 6. KBO CLI/Playwright 장애 복구 Adapter (P3)

- [ ] `KboCliSource`를 구현한다.
- [ ] `KboPlaywrightSource`를 구현한다.
- [ ] HTTP schema 변경·전송 실패 시에만 CLI fallback을 실행한다.
- [ ] CLI 실패 시에만 Playwright fallback을 실행한다.
- [ ] 정상적인 0건 응답에는 fallback을 실행하지 않는다.
- [ ] fallback별 종료 코드·오류 로그·테스트를 추가한다.

### 7. 수집 관측성 강화 (P3)

- [ ] 공급자별 성공·실패 카운터를 추가한다.
- [ ] 경기별 마지막 성공 수집 시각을 노출한다.
- [ ] fallback 횟수와 실패 사유를 저장한다.
- [ ] 관리자 화면에서 최근 수집 작업을 조회한다.
- [ ] 관리자 화면에서 최근 오류를 조회한다.

## 완료 처리 규칙

- 구현만 끝난 항목은 체크하지 않는다.
- 테스트와 실제 서버 검증까지 끝난 항목만 체크한다.
- 마이그레이션이 필요한 경우 서버 DB 적용까지 완료해야 한다.
- API 동작과 문서가 일치하는지 확인한 뒤 체크한다.
- 각 단계 완료 시 커밋에 해당 체크리스트 항목을 기록한다.

## 상세 설명

### 실시간 투수·타자·카운트·주자 상태

네이버 스포츠 relay 원본의 `currentGameState`를 사용해 현재 타석 상태를 수집하도록 구현했습니다.

- [x] 현재 투수와 타자 ID·이름을 파싱한다.
- [x] 볼·스트라이크·아웃 카운트를 파싱한다.
- [x] 1·2·3루 주자 ID·이름을 파싱한다.
- [x] 기존 scoreboard JSON에 `liveState`를 결합한다.
- [x] `/api/v1/games`의 `score.live`로 노출한다.
- [x] relay 응답 파싱 단위 테스트를 추가한다.
- [ ] 실제 경기중 서버에서 카운트와 주자 상황을 검증한다.
- [ ] Swagger와 관리자 화면에서 실시간 상태를 확인한다.

네이버 스포츠 endpoint는 공식 개발자 Open API가 아니므로, 응답 변경 시 해당 adapter만 수정합니다.

### 기준 날짜 이후 최신 순위 fallback

`GET /api/v1/rankings?date=YYYY-MM-DD`는 해당 날짜의 스냅샷이 없으면 요청일 이전의 가장 최근 스냅샷을 반환하도록 구현했습니다. 실제 서버 검증 후 항목을 완료 처리합니다.

개선 방향:

- 요청 날짜 이하(`as_of_date <= date`)의 가장 최근 순위 선택
- 응답 `asOfDate`에는 실제 사용한 스냅샷 날짜 표시
- 요청 날짜보다 이전 데이터만 존재하는 경우 `stale` 또는 `dataAge` 메타데이터 제공

완료 기준:

```text
GET /api/v1/rankings?date=2026-09-19
→ asOfDate: 2026-09-18
→ rankings: 10개 팀
```

### 종료 경기 최종 R/H/E/BB 보강

현재 실시간 경기에는 `hits`, `errors`, `walks`가 채워지지만, 종료 경기의 `/api/v1/results/latest`에서는 해당 값이 `null`일 수 있습니다.

개선 방향:

- 경기 종료 감지 후 최종 스코어보드 1회 수집
- KBO 상세 스코어보드가 비어 있으면 네이버 스포츠 데이터 fallback
- 재수집 시 기존 실시간 스코어보드를 최종 데이터로 갱신
- 수집 출처를 `scoreboardSource`로 유지

완료 기준:

```json
{
  "score": {
    "away": 4,
    "home": 1,
    "hits": {"away": 8, "home": 5},
    "errors": {"away": 0, "home": 1},
    "walks": {"away": 3, "home": 2},
    "scoreboardSource": "kbo-scoreboard"
  }
}
```

### 경기 투수 결과 표준화

현재 `/api/v1/games/{gameId}/details`의 `pitchers[].stats`에 KBO 원문 키가 저장됩니다.

예:

```json
{"승":"7", "패":"1", "세":"0", "홀드":"0"}
```

개선 방향:

- `win`, `loss`, `save`, `hold` 필드를 별도로 제공
- 기존 `stats` 원문은 하위 호환을 위해 유지
- 승리투수·패전투수·세이브투수를 경기 요약에도 노출

### Swagger 응답 모델 확대

현재 경기 목록·단일 경기 응답 모델은 추가했지만, 순위·선수 기록·MVP·라인업·상세 기록 API는 일부가 동적 `dict` 응답입니다.

개선 방향:

- 모든 공개 API에 Pydantic `response_model` 추가
- Swagger에서 필드 타입·필수 여부·예시 노출
- 오류 응답 형식도 공통 스키마화

### 수집 출처 추상화 확대

현재 공급자 구조는 KBO 공식 데이터와 네이버 실시간 스코어보드 fallback을 지원합니다.

개선 방향:

- KBO HTTP, 네이버 스포츠, 향후 다른 공급자를 동일한 Port로 추상화
- 공급자별 요청 성공률·실패 원인·마지막 성공 시각 기록
- 네이버 내부 endpoint 변경 시 어댑터만 교체

주의:

- 네이버 스포츠 endpoint는 공식 개발자 Open API가 아닙니다.
- 개인용 fallback으로 사용하며 응답 변경과 호출 제한 가능성을 고려해야 합니다.

### KBO CLI/Playwright 장애 복구 Adapter

구현지침에는 HTTP 실패 시 CLI, 최종적으로 Playwright를 사용하는 fallback이 정의되어 있으나 현재 실제 구현은 KBO HTTP와 네이버 scoreboard fallback 중심입니다.

개선 방향:

- `KboCliSource` 구현
- `KboPlaywrightSource` 구현
- HTTP schema 변경·전송 실패 시에만 fallback
- 정상적인 0건 응답에는 fallback을 실행하지 않음

### 수집 관측성 강화

개선 방향:

- 공급자별 수집 성공/실패 카운터
- 경기별 마지막 성공 수집 시각
- fallback 횟수와 실패 사유
- 관리자 화면에서 최근 수집 작업·오류 조회

## 운영 전 확인 항목

- `.env`의 `SCOREBOARD_PROVIDER=hybrid` 확인
- `NAVER_SPORTS_ENABLED=true` 확인
- Worker가 실행 중인지 확인
- 경기중 API의 `score.scoreboardSource` 확인
- 종료 후 최종 R/H/E/BB가 저장됐는지 확인
- 순위 응답의 `asOfDate`가 실제 수집 날짜인지 확인

## 검증 명령

```bash
curl -s http://localhost:8085/api/v1/rankings
curl -s 'http://localhost:8085/api/v1/games?date=YYYY-MM-DD'
curl -s 'http://localhost:8085/api/v1/results/latest?limit=5'
curl -s http://localhost:8085/openapi.json
```
